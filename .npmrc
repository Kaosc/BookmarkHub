# react-scripts@5 declares an advisory peer of typescript ^3.2.1 || ^4,
# but @dnd-kit v0.3+ ships .d.ts files that require TypeScript 5+.
# CRA has no runtime TS version check, so relax npm's strict peer resolution.
legacy-peer-deps=true
